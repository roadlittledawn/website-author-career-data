# Authentication Configuration
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here
AUTH_SECRET=your_jwt_secret_here

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/career-data

# Anthropic API Configuration
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Instructions:
# 1. Copy this file to .env
# 2. Generate password hash: node scripts/generate-password-hash.js <your_password>
# 3. Generate a secure JWT secret (e.g., openssl rand -base64 32)
# 4. Update all values with your actual credentials
